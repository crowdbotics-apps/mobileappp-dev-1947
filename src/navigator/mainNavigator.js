import { createAppContainer } from 'react-navigation';
import { createStackNavigator } from 'react-navigation-stack';
import {createDrawerNavigator} from 'react-navigation-drawer';

import SplashScreen from "../features/SplashScreen";
import SideMenu from './sideMenu';
//@BlueprintImportInsertion
import UserProfile507Navigator from '../features/UserProfile507/navigator';
import UserProfile506Navigator from '../features/UserProfile506/navigator';
import EmailAuth505Navigator from '../features/EmailAuth505/navigator';
import EmailAuth500Navigator from '../features/EmailAuth500/navigator';
import EmailAuthNavigator from '../features/EmailAuth/navigator';

/**
 * new navigators can be imported here
 */

const AppNavigator = {
    SplashScreen: {
      screen: SplashScreen
    },
    //@BlueprintNavigationInsertion
UserProfile507: { screen: UserProfile507Navigator },
UserProfile506: { screen: UserProfile506Navigator },
EmailAuth505: { screen: EmailAuth505Navigator },
EmailAuth500: { screen: EmailAuth500Navigator },
EmailAuth: { screen: EmailAuthNavigator },

    /** new navigators can be added here */
};

const DrawerAppNavigator = createDrawerNavigator(
  {
    ...AppNavigator,
  },
  {
    contentComponent: SideMenu,
    initialRouteName: 'SplashScreen',
  },
);

const AppContainer = createAppContainer(DrawerAppNavigator);

export default AppContainer;
